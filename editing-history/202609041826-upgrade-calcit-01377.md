# Upgrade calcit-ir-viewer to Calcit 0.13.77

- Raise the compiler baseline from 0.13.33 to 0.13.77 and pin the JavaScript runtime package to the exact matching version.
- Resolve Calcit modules strictly and install JavaScript dependencies immutably before verifying the exact linked toolchain.
- Pin the existing setup-calcit 1.3.0 action to its immutable commit and restrict the workflow token to read-only repository contents.
- Keep deployment push-only while preserving viewer behavior, application version 0.0.2, and the existing static analysis gates.

## Dependency alignment and validation

- Yarn 4.12.0 immutable installation and exact Calcit/procs 0.13.77 toolchain verification pass.
- Align Reel 0.6.19, respo-markdown 0.4.33, respo-ui 0.7.19, Respo 0.16.95, Lilac 0.5.9, and memof 0.0.31 to their compatible published graph.
- Upgrade Vite from the old prerelease range to the current 8.2.2 release line.
- The root Snapshot needs no legacy dual-snapshot migration; the complete strict dependency, static-analysis, JavaScript, and Vite workflow is now validated against formal releases.
- Adapt open IR map reads to Option semantics, validate string/tag discriminators at typed adapters, decode persisted state into `StoreData`, and keep Struct updates nominal.
- Clear all 36 preprocessing warnings and all deprecated/dynamic method calls; commit the reviewed per-definition quality baseline to prevent debt growth.

## 中文

- 将编译器基线从 0.13.33 升级到 0.13.77，并把 JavaScript runtime package 精确固定到相同版本。
- 先执行 strict Calcit 模块解析和 immutable JavaScript 依赖安装，再核对精确且已链接的工具链。
- 将既有 setup-calcit 1.3.0 action 固定到不可变 commit，并把 workflow token 限制为只读仓库内容。
- 保持仅 main push 部署，同时维持 viewer 行为、应用版本 0.0.2 和既有静态分析门禁。

## 依赖对齐与验证

- Yarn 4.12.0 immutable 安装及 Calcit/procs 精确 0.13.77 工具链核对均通过。
- 对齐已正式发布的 Reel 0.6.19、respo-markdown 0.4.33、respo-ui 0.7.19、Respo 0.16.95、Lilac 0.5.9 与 memof 0.0.31 兼容依赖图。
- 将 Vite 从旧 prerelease 范围升级到当前 8.2.2 release line。
- 根 Snapshot 不需要旧双快照迁移；完整 strict 依赖、静态分析、JavaScript 与 Vite 流程现已基于正式发布版本验证。
- 将开放 IR Map 读取迁移到 Option 语义，在 typed adapter 中验证 string/tag discriminator，将持久化状态解码为 `StoreData`，并保持 Struct 更新的 nominal 类型。
- 清除全部 36 条预处理 warning，以及全部 deprecated/dynamic method 调用；提交逐 definition 的质量 baseline，阻止债务增长。
